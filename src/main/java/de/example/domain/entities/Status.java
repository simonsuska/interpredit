package de.example.domain.entities;

public enum Status {
    OK,
    OUTPUT,
    INPUT,
    SET_ERROR,
    MEMORY_ADDRESS_ERROR,
    COMMAND_ERROR,
    DECODE_ERROR,
    DIVISION_BY_ZERO_ERROR,
    FINISH
}
